version: '3'
services:
  app:
    image: sms-2-openai-img
    build:
      context: ../
      dockerfile: ./dev/Dockerfile
    container_name: sms-2-openai-app
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - "../app:/usr/src/host-app"
    working_dir: "/usr/src/host-app"
